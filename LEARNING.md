# Data Input Agent 학습 가이드

## 1. 이 예제를 만든 맥락

이 예제는 언어모델을 사용한 에이전트가 규칙 기반의 코드 대비 유용할 수 있는 사례를 제시하기 위해 제작되었습니다.
외부로부터 확보 가능한 문서는 같은 의미를 담고 있어도 형식이 일정하지 않을 수 있습니다.
단순한 열 위치나 정규식만으로 처리하면 문서 형식이 바뀔 때마다 규칙을 다시 작성해야 합니다.
본 에이전트는 로컬 언어 모델을 활용하여 이를 개선합니다.


이 프로젝트는 서로 전혀 다른 두 발전기 정비 공지에서 아래 네 값을 찾아 같은 CSV로 만드는 작은 예제입니다.

- 어떤 발전기인지
- 정비 시작 시각
- 정비 종료 시각
- 근거가 된 PDF 파일명

샘플 PDF에는 인사말, 협조 요청, 관련 없는 날짜처럼 추출 대상이 아닌 문장도 들어 있습니다. 에이전트는 문서의 표현과 배치가 달라도 의미를 해석해 필요한 정보만 골라야 합니다.

## 2. 왜 에이전트인가

보통의 함수는 개발자가 정한 순서대로만 실행됩니다. 이 예제의 에이전트는 목표와 사용할 수 있는 툴을 받은 뒤, 현재 상황에 맞춰 다음 행동을 선택합니다.

```text
목표를 받음
  -> PDF 읽기 툴 선택
  -> 추출된 텍스트 관찰
  -> 필요한 값 해석
  -> CSV 쓰기 툴 선택
  -> 저장 결과 관찰
  -> 완료 답변
```

핵심은 모델이 직접 파일 시스템을 마음대로 조작하지 않는다는 점입니다. 모델은 “어떤 툴을 어떤 인자로 호출할지” 제안하고, 실제 Python 프로그램이 허용 여부를 확인한 뒤 실행합니다.

## 3. 구성 요소의 역할

### Ollama

로컬 컴퓨터에서 언어 모델을 실행하고 Python 코드의 요청에 응답하는 모델 실행기입니다. 이 프로젝트는 외부 LLM API나 API 키를 사용하지 않습니다.

### Qwen3 4B

PDF에서 추출된 글을 이해하고, 발전기 이름과 정비 기간을 판단하며, 다음에 사용할 툴을 선택하는 로컬 언어 모델입니다. 기본 모델은 `qwen3:4b`입니다.

### `agent.py`

사용자의 목표, 대화 기록, 로컬 모델, 툴을 연결하는 실행 프로그램입니다. 모델 응답에 툴 호출이 있으면 해당 Python 함수를 실행하고 결과를 다시 모델에게 전달합니다.

### 툴

툴은 모델이 현실의 데이터에 접근하거나 변경할 때 사용하는 제한된 Python 함수입니다.
에이전트가 활용할 수 있는 툴을 정의하므로써 에이전트에게 허용된 행위가 제한됩니다.
참고로 또 다른 에이전트(subaget)를 부르는 것도 툴이 될 수 있습니다.

| 툴 | 하는 일 | 제한 |
| --- | --- | --- |
| `extract_pdf_text` | 허용된 입력 폴더의 일반 PDF를 텍스트로 변환 | 임의 파일 읽기 방지 |
| `append_maintenance_csv` | 네 필드를 검증하고 CSV에 한 행 추가 | 잘못된 날짜와 출처 저장 방지 |

모델에게 범용 파일 읽기·쓰기 권한을 주지 않고, 목적에 맞는 좁은 툴만 제공하는 것이 안정성을 위해 중요한 설계 원칙입니다.

## 4. 에이전트 반복문의 구조

`run_agent_for_pdf()` 안의 반복문이 가장 작은 형태의 에이전트 런타임입니다.

```text
while 최대 반복 횟수 이내:
    모델에게 현재 대화와 툴 목록 전달
    모델 응답 수신

    if 툴 호출이 없으면:
        최종 답변을 출력하고 종료

    선택된 툴이 허용 목록에 있는지 확인
    툴 실행
    툴 결과를 대화 기록에 추가
    다음 모델 호출에서 결과를 관찰하게 함
```

이 구조 때문에 모델은 한 번의 텍스트 생성만 하는 것이 아니라, 행동 결과를 보고 다음 행동을 이어갈 수 있습니다. 
무한 반복을 막기 위해 최대 횟수는 5회로 제한하였습니다.

## 5. 예제에서 주어진 툴 설명

### PDF 읽기 툴

`extract_pdf_text()`는 요청받은 경로가 사용자가 지정한 입력 폴더 안인지 확인합니다. 확장자가 PDF인지, 파일이 실제로 존재하는지도 검사합니다. 각 페이지의 텍스트에는 페이지 표시를 붙여 모델에게 반환합니다.

이 예제는 일반 PDF만 다룹니다. 페이지에 글자가 보이더라도 실제로는 이미지인 스캔 PDF라면 별도의 OCR 툴이 필요합니다.

### CSV 쓰기 툴

`append_maintenance_csv()`는 모델의 판단을 그대로 믿고 저장하지 않습니다. 다음 조건을 코드로 다시 검사합니다.

- 발전기 이름이 비어 있지 않은가
- 시작과 종료가 모두 있는가
- 날짜 형식이 `YYYY-MM-DD HH:MM`인가
- 종료가 시작보다 빠르지 않은가
- 출처가 현재 처리 중인 PDF인가
- 완전히 같은 행이 이미 저장되어 있지 않은가

언어 모델은 의미 해석을 담당하고, 명확한 데이터 규칙은 일반 코드가 담당하도록 책임을 나눈 것입니다.

## 6. 단계별 pause 단계 (Enter로 진행) 

기본 실행에서는 다음 시점마다 멈춥니다.

1. 모델에 작업을 전달하기 전
2. 모델이 선택한 툴을 실행하기 전
3. 툴 결과를 모델에게 돌려주기 전

로그에는 모델 요청, 툴 이름, 툴 인자, 툴 결과처럼 프로그램 밖에서 확인 가능한 행동만 표시합니다. 모델의 숨겨진 내부 사고과정을 출력하는 기능은 아닙니다.

## 7. `agent.py`를 읽는 순서

코드의 `STEP` 주석을 따라 읽으면 됩니다.

1. 모델 이름, CSV 양식, 시스템 규칙
2. 발표 중 흐름을 멈추는 함수
3. PDF 텍스트 추출 툴
4. CSV 검증·저장 툴
5. 로그를 읽기 쉽게 표시하는 함수
6. 모델과 툴을 연결하는 에이전트 반복문
7. 입력 폴더의 PDF를 순서대로 처리하는 명령행 진입점


## 8. 샘플 문서

두 PDF는 실제 운영자료가 아닌 임의로 생성한 문서입니다.

- `maintenance_notice_a.pdf`: 문서번호, 수신처, 협조 요청이 포함된 공문 형식
- `maintenance_notice_b.pdf`: 핵심 정보가 여러 카드와 표에 나뉜 현장 브리핑 형식

레이아웃, 문체, 관련 없는 정보가 서로 다르지만 사람이 읽으면 정비 대상과 기간을 명확히 판단할 수 있습니다. 
불필요한 정보를 생략하고 정해진 출력 스키마를 생성하는 과정이 비정형 입력에 대한 에이전트의 장점을 보여줍니다.

## 9. 한계와 다음 실습

예제의 범위는 의도적으로 매우 작게 설정되었습니다.
실제 시스템에 적용할 경우 더 큰 프로세스의 맥락 안에서 각 에이전트들이 주어진 역할을 수행하게 될 것으로 예상할 수 있습니다.
구조가 복잡해질 경우 오케스트레이터(지휘자)가 서브에이전트(일꾼)들이 분리된 구조 또한 상상할 수 있습니다.
